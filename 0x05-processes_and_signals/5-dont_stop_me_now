#!/usr/bin/env bash
# Stop the 4-to_infinity_and_beyond process

pids=$(pgrep -f 4-to_infinity_and_beyond)

# Kill each PID if found
if [ -n "$pids" ]; then
    for pid in $pids; do
        kill "$pid" 2>/dev/null
    done
fi
